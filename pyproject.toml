[build-system]
requires = ["setuptools"]
build-backend = "setuptools.build_meta"

[tool.setuptools.packages.find]
include = ["raptor*"]

[project]
name = "raptor"
version = "0.1.0"
requires-python = ">=3.8"
dependencies = [
    "faiss-cpu",
    "numpy",
    "openai",
    "scikit-learn",
    "scipy",
    "sentence-transformers",
    "tenacity",
    "tiktoken",
    "torch",
    "transformers",
    "umap-learn",
]
